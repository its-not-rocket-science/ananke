# Ananke v1.0.0-beta.1 — Beta Launch Readiness Checklist

> Gate owner: Release Manager / QA Lead  
> Status source of truth: `.github/beta-launch-readiness.json`

## 1) Technical readiness

- [ ] **All tests pass on Node 18/20/22, Windows/macOS/Linux**
  - Evidence: GitHub Action `beta-launch-readiness.yml` → `technical-matrix`.
- [ ] **Determinism fuzzer: 10,000 / 10,000 seeds pass**
  - Evidence: `npm run test:determinism:nightly` in `determinism-fuzzer` job.
- [ ] **Performance benchmarks meet budget** (`.github/perf-budget.json`)
  - Evidence: `npm run benchmark:run && npm run benchmark:budget` in `performance-budget` job.
- [ ] **No memory leaks (48-hour soak test)**
  - Evidence: soak report linked in `.github/beta-launch-readiness.json`.
- [ ] **WASM backend loads and passes determinism suite**
  - Evidence: `npm run test:wasm` and wasm determinism test in `wasm-determinism` job.

## 2) API stability

- [ ] **Tier 1 exports frozen (API diff since v0.1.69 = empty)**
  - Evidence: `api-stability` job (Tier 1 surface check + diff report).
- [ ] **All Tier 1 functions have JSDoc examples**
  - Evidence: API doc generation + manual QA signoff in `.github/beta-launch-readiness.json`.
- [ ] **Deprecation policy documented**
  - Evidence: `docs/api-lifecycle.md`.

## 3) Documentation

- [ ] **4 learning paths complete and tested by fresh user**
  - Evidence: user test log links in `.github/beta-launch-readiness.json`.
- [ ] **API reference generated and searchable**
  - Evidence: `npm run docs:api` and docs site verification.
- [ ] **Video tutorial published (YouTube)**
  - Evidence: published URL stored in `.github/beta-launch-readiness.json`.

## 4) Adoption readiness

- [ ] **3 external users have built prototypes (signed NDA or public)**
  - Evidence: external prototype links or NDA IDs in `.github/beta-launch-readiness.json`.
- [ ] **0 P1 bugs reported in last 14 days**
  - Evidence: issue tracker query snapshot linked in `.github/beta-launch-readiness.json`.
- [ ] **Support channel (Discord) has 10+ active members**
  - Evidence: analytics screenshot/report link in `.github/beta-launch-readiness.json`.

## 5) Legal & governance

- [ ] **LICENSE file correct (MIT/Apache 2.0)**
  - Evidence: `LICENSE` validated in `manual-attestation-gate` job.
- [ ] **CLA signed by all contributors (if required)**
  - Evidence: CLA report link and signoff in `.github/beta-launch-readiness.json`.
- [ ] **Security policy published**
  - Evidence: `SECURITY.md` present and reviewed.

## Exit criteria

**Do not announce `v1.0.0-beta.1` unless all checkboxes are complete and the `Beta Launch Readiness / release-gate` job is green.**
