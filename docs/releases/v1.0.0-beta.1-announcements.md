# Beta Launch Announcement Drafts — Ananke v1.0.0-beta.1

> Replace placeholders before posting:
> - `{{GITHUB_URL}}`
> - `{{DOCS_URL}}`
> - `{{NPM_URL}}`
> - `{{CHANGELOG_URL}}`
> - `{{DISCORD_URL}}`

## Hacker News draft (Show HN)

**Title:** Show HN: Ananke v1.0.0-beta.1 — deterministic lockstep simulation engine (TS + WASM)

We just launched **Ananke v1.0.0-beta.1**, a deterministic simulation core for lockstep games and reproducible campaign/world systems.

What’s in this beta:
- Deterministic lockstep-first architecture (replayable, seedable sim)
- TypeScript host runtime with optional WASM backend
- Tier 1 API stability gates (including diff checks since v0.1.69)
- Benchmark budget + determinism CI gates
- Four learning paths and reference examples

Who this is for:
- Multiplayer/lockstep game developers
- Simulation-heavy strategy/RPG teams
- Tooling teams that need reproducible server-side simulation

If you try it, we’d especially love feedback on:
1. API ergonomics
2. Determinism workflow in real projects
3. Performance on your scenarios vs. our benchmark suite

Links:
- GitHub: {{GITHUB_URL}}
- Docs: {{DOCS_URL}}
- npm: {{NPM_URL}}
- Changelog: {{CHANGELOG_URL}}
- Discord: {{DISCORD_URL}}

## Reddit draft (r/gamedev or r/programming)

**Title:** We’re launching Ananke v1.0.0-beta.1 (deterministic sim engine for lockstep games) — feedback welcome

Hey all — we’re releasing **Ananke v1.0.0-beta.1** and looking for beta feedback.

Ananke is a deterministic simulation engine for teams building:
- lockstep multiplayer combat,
- reproducible campaign/world simulation,
- replay/debug pipelines where exact determinism matters.

Highlights in this beta:
- TS runtime + optional WASM backend
- Determinism fuzzing and regression tests in CI
- Performance budget checks
- Tier 1 API stability gating
- Reference examples + docs learning paths

If anyone wants to kick the tires, we’d love practical feedback from real prototypes (perf, DX, migration friction, docs gaps).

Links:
- Repo: {{GITHUB_URL}}
- Quickstart docs: {{DOCS_URL}}
- npm: {{NPM_URL}}
- Changelog: {{CHANGELOG_URL}}
- Discord: {{DISCORD_URL}}

Thanks — happy to answer technical questions in-thread.

## Twitter/X draft

🚀 **Ananke v1.0.0-beta.1 is out!**

A deterministic simulation engine for lockstep games + reproducible world/campaign systems.

✅ TS runtime + WASM backend option  
✅ Determinism fuzzing + API diff gates  
✅ Perf budget CI + reference examples

Looking for beta users building real prototypes.

Docs: {{DOCS_URL}}<br>
GitHub: {{GITHUB_URL}}<br>
npm: {{NPM_URL}}

#gamedev #webdev #typescript #wasm #indiedev

## Optional short Twitter/X variant (fits tighter character budget)

Ananke v1.0.0-beta.1 is live 🚀<br>
Deterministic sim engine for lockstep games (TS + WASM), with API diff, determinism, and perf-budget gates in CI.

Docs: {{DOCS_URL}}<br>
GitHub: {{GITHUB_URL}}
