name: Determinism

on:
  pull_request:
  push:
    branches: [main]

jobs:
  regression:
    name: Determinism regression gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22.x"
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Run determinism regression test (json)
        run: npm run test:determinism -- --run test/determinism/regression.spec.ts --seed=1337 --reporter=json --outputFile=determinism-report/results.json
      - name: Build determinism summary artifact
        run: node tools/generate-determinism-summary.mjs --input=determinism-report/results.json --output=determinism-report/summary.json
      - name: Upload determinism summary artifact
        uses: actions/upload-artifact@v4
        with:
          name: determinism-summary
          path: determinism-report/summary.json
